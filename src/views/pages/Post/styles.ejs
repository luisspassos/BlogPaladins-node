<style>

main {
    color: var(--BLUE-TERTIARY);
}

.noticia {
    align-items: center;
    display: flex;
}

.noticia  > figure {
    max-width: 750px;
    height: 393.75px;
}

.noticia > figure > img {
    width: 100%;
    height: 100%;
    border-radius: 0px 10px 10px 0px;
    object-fit: cover;
}

.postTitle {
    padding-left: 40px;
    margin-right: 20px;
}

.titulo-main {
    font-weight: bold;
    font-size: 2.7em;
    max-width: 500px;
}

.titulo-main-destaque {
    background-color: var(--BLUE-TERTIARY);
    color: white;
    font-weight: bold;
    padding: 3px 8px;
    font-size: .8em;
    border-radius: 15px;
    margin-bottom: 10px;
    display: inline-block;
}

.titulo-main-destaque > i {
    color: white;
}

.autor-main {
    padding-top: 15px;
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    gap: 5px 15px;
}

.autor-main > time {
    display: inline-flex;
    align-items: center;
    gap: 8px;
}

.main-tempo-leitura {
    font-size: .9em
}

.conteudo {
    margin: auto;
    max-width: 700px;
    padding: 20px;
    font-size: 1.2em;
    line-height: 2em;
}

.conteudo > h1 {
    padding: 20px 0px;
    font-size: 1.4em;
}

.conteudo > p {
    text-align: justify;
    padding-bottom: 20px;
}

.conteudo > ul {
    list-style-position: inside;
}

.conteudo a {
    text-decoration: none;
    border-bottom: 3px solid var(--GRAY-PRIMARY);
    color: var(--BLUE-TERTIARY);
    word-break: break-all;
}

.conteudo img {
    max-width: 700px;
    width: 100%;
}

.conteudo a:hover {
    border-bottom: 3px solid var(--BLUE-SECONDARY);
}

@media (max-width: 900px) {

    .postTitle {
        position: absolute;
        color: white;
    }

    .postTitle > p:nth-child(2),
    .postTitle > p:nth-child(3) {
        text-shadow: var(--TEXT-SHADOW-PRIMARY);
    }

    .noticia  > figure {
        width: 100%;
        max-width: initial;
    }

    .noticia > figure > img {
        border-radius: 0;
    }
}
</style>


