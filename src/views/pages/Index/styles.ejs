<style>

    main {
        margin: 0 40px 25px;
    }

    .emphasis {
        display: flex;
        align-items: center;
        gap: 25px;
    }

    .emphasis > h1 {
        font-size: 2.6em;
    }

    .slider {
        overflow: hidden;
        max-width: 800px;
        height: 400px;
        border-radius: 15px;
        position: relative;
        display: flex;
        align-items: center;
        box-shadow: var(--SHADOW-PRIMARY);
    }

    .imgsSlider {
        display: flex;
        transform: translateX(0);
        transition: .5s ease-in-out;
    }

    .imgsSlider > li {
        display: flex;
        flex-direction: column;
        flex-shrink: 0;
        width: 100%;
        height: 400px;
        list-style: none;
        position: relative;
    }

    .imgsSlider > li:hover > img {
        transform: scale(1.1);
    }

    .imgsSlider > li > img {
        width: 100%;
        height: 400px;
        object-fit: cover;
        cursor: pointer;
        transition: .3s;
    }

    .imgsSlider > li > h2 {
        color: white;
        text-shadow: var(--TEXT-SHADOW-PRIMARY);
        text-align: center;
        font-size: 2em;
        align-self: center;
        max-width: 600px;
        position: absolute;
        bottom: 25px;
        pointer-events: none;
    }

    .slider > button {
        background: transparent;
        position: absolute;
        border: 0;
        font-size: 3.9em;
        height: 100%;
        padding: 0 20px;
        cursor: pointer;
        transition: .2s;
    }

    .slider > button:hover {
        font-size: 4.5em;
    }

    .slider > button > i {
        color: var(--BLUE-PRIMARY);
        text-shadow: var(--SHADOW-PRIMARY);
    }

    .slider > button:nth-of-type(1) {
        left: 0;
        border-radius: 15px 0 0 15px;
    }

    .slider > button:nth-of-type(1):hover {
        background: linear-gradient(to right, var(--SHADOW-SLIDER-HOVER), transparent);
    }

    .slider > button:nth-of-type(2) {
        right: 0;
        border-radius: 0 15px 15px 0;
    }

    .slider > button:nth-of-type(2):hover {
        background: linear-gradient(to right, transparent, var(--SHADOW-SLIDER-HOVER));
    }

    .postList {
        list-style: none;
        padding-top: 25px;
        display: flex;
        justify-content: center;
        flex-wrap: wrap;
        gap: 25px;
    }

    .postList > li {
        width: 310px;
        height: 380px;
        display: flex;
        flex-direction: column;
        justify-content: end;
        gap: 3px;
        box-shadow: var(--SHADOW-PRIMARY);
        cursor: pointer;
        border-radius: 20px;
        overflow: hidden;
    }

    .postList > li > figure {
        background: url(https://pbs.twimg.com/media/Elq1og6XIAMg7vA.jpg) 75% 10%;
        width: 100%;
        border-radius: 20px;
        height: 100%;
        transition: .2s;
    }

    .postList > li:hover > figure {
        transform: scale(1.1);
    }

    .postList > li > h3 {
        font-size: 1.6em;
    }

    .postList > li > p:nth-of-type(1) {
        word-spacing: 2px;
    }

    .postList > li > *,
    .postList > li > p > *{
        color: white;
        text-shadow: var(--TEXT-SHADOW-PRIMARY);
    }

</style>