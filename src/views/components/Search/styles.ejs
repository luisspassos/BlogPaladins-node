<style>

.search {
    position: relative;
    margin-left: 25px;
}

.search > img {
    width: 16px;
    cursor: pointer;
    display: block;
    margin: 0 0 3px auto;
}

.search > img:hover {
    transform: scale(1.1);
}

.search > input {
    width: 330px;
    font-size: 1.2em;
    border: 0;
    outline: 0;
}

.search > input::-webkit-search-cancel-button {
    display: none;
}

.searchInputLines {
    display: flex;
    flex-direction: row-reverse;
    overflow: hidden;
}

.searchInputLines > hr {
    border: 0;
    width: 100%;
    height: 3px;
    flex-shrink: 0;
}

.searchInputLines > hr:nth-of-type(1) {
    background: var(--GRAY-SECONDARY);
}

.searchInputLines > hr:nth-of-type(2) {
    background: var(--BLUE-PRIMARY);
    transform: translateX(0);
    transition: .3s;
}

.searchInputLines> input::-webkit-search-cancel-button {
    display: none;
}

.search > input:focus + div > hr:nth-of-type(2) {
    transform: translateX(100%);
}

.search > ul {
    display: none;
    flex-direction: column;
    gap: 16px;
    background: var(--GRAY-TERTIARY);
    color: white;
    font-size: 1.1em;
    border-radius: 0 0 10px 10px;
    padding: 13px;
    box-shadow: var(--SHADOW-PRIMARY);
    list-style-type: none;
    position: absolute;
    z-index: 3;
}

.search > input:focus ~ ul {
    display: flex;
}

.search > ul > li {
    font-weight: normal;
}

@media (max-width: 719px) {
    .search {
        width: 100%;
        margin: 5px 0 0 0;
    }

    .search > input {
        width: 100%;
    }

    .search > ul {
        width: 100%;
    }
}

</style>