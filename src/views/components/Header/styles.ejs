<style>

header {
    margin: 40px;
    display: flex;
    align-items: center;
    justify-content: space-between;
    font-weight: bold;
}

header > div {
    display: flex;
    align-items: center;
}

header > div > a {
    text-decoration-line: none;
    display: flex;
    align-items: center;
}

header > div > a > p  {
    padding-left: 15px;
    font-size: 1.3em;
    line-height: 1.7em;
    color: var(--BLUE-TERTIARY);
}

header > div > a > p > strong {
    background-color: var(--BLUE-PRIMARY);
    color: white;
    padding: 5px;
    border-radius: 10px;
    margin-left: 5px;
}

.img-header {
    max-width: 70px;
}

.hamburgerMenu {
    display: none;
}

header > nav > a {
    text-decoration-line: none;
    padding-left: 15px;
    color: var(--BLUE-TERTIARY);
}

header > nav > a:hover {
    text-decoration-color: var(--BLUE-TERTIARY);
    text-decoration-line: underline;
}

header > nav > a > i:hover {
    color: var(--BLUE-PRIMARY);
}

@media screen and (max-width: 782px) {
    
    header > nav {
        display: none;
    }

    .hamburgerMenu {
        display: block;
        font-size: 2.7em;
        cursor: pointer;
        color: var(--BLUE-TERTIARY);
    }
}

@media screen and (max-width: 460px){
    
    header {
        align-items: flex-start;
        flex-direction: column-reverse;
    }

    .hamburgerMenu {
        margin-bottom: 15px;
    }
}

@media (max-width: 347px) {
    header {
        margin: 40px 25px;
    }
}

</style>